import 'package:flutter/material.dart';
class Urgence extends StatelessWidget {
  const Urgence({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Retrait d'urgence"),
      ),
      body: const Column(
        children: [

        ],
      ),
    );
  }
}
